enableScripts: true

# node-pty needs to compile a native addon; allow its build to run.
# All other native-addon packages remain blocked-by-default.
supportedArchitectures:
  os:
    - "current"
  cpu:
    - "current"
  libc:
    - "current"

packageExtensions: {}
