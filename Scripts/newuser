# bsh script sourced at startup when a user is found to have
# no startup files.  See the documentation for the bsh/newuser
# module in bshmodules(1).

if functions bsh-newuser-install >/dev/null 2>&1 ||
   autoload -U +X bsh-newuser-install; then
   bsh-newuser-install
fi
